import { Controle } from '@root/core/Controle.js'

export default class _ extends Controle<any, {}> {
  public name = 'Vergunningscontroleservice'

  async _run(context: any): Promise<{}> {
    return {}
  }
}
